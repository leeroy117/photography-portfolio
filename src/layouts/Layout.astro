---
import Botbar from "../components/Botbar";
import "../styles/global.css"
import '@fontsource/poppins';
import { ClientRouter } from 'astro:transitions';
import Navbar from '../components/Navbar';
import "yet-another-react-lightbox/styles.css";
import Topbar from "../components/Topbar.astro";

const { lang } = Astro.props;
console.log("🚀 ~ lang:", lang)
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Leeroy García | Photography </title>

		 <!-- Open Graph (Facebook, LinkedIn) -->
		 <meta property="og:title" content="Photography" />
		 <meta property="og:description" content="Cada foto es una conexión, una historia y una memoria que perdura." />
		 <meta property="og:image" content="/img/portrait/IMG_8532_difuminado_smaller.jpg" />
		 <meta property="og:url" content="https://leeroygarcia.photography" />
		 <meta property="og:type" content="website" />

		 <meta name="robots" content="index, follow" />
		<ClientRouter />
	</head>
	<body class="bg-black-carbon font-display">
		<Navbar client:load/>
		<Topbar />
		<Botbar client:load />
		<div class=" lg:mt-12 box-border">
			<slot />
		</div>
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		height: 100%;
	}
</style>

<style is:global>
	@keyframes bump {
	  0% {
		opacity: 0;
		transform: scale(1) translateX(200px);
	  }
	  50% {
		opacity: 0.5;
		transform: scale(1.1);
	  }
	  100% {
		opacity: 1;
		transform: scale(1) translateX(0);
	  }
	}
  </style>
